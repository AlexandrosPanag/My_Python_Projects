s = 'Word'
print(len(s.strip())) # this line outputs the size of the string

print(s.upper()) # this line outputs the word in capital form

print(len('This is a string text'.split())) #it will output the words seperately