name= input("Enter username: ") #we print a text & receive user input and then store into the name variable
print("Player has joined, "+name) #we print a text along with the received user input and then we print the variable
