import numpy as np #importing numpy you might need to run the command <pip install numpy>, in case it is not
#recognized by visual studio code , set the numpy name to np

numbers = np.array([2,3,5,6,11]) #create a numpy array

print(type(numbers)) #notice how the type is printed as class named numpy.ndarray
print(np.ndarray) 

print(numbers) #however the array is printed normally
