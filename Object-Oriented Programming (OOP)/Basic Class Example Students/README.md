Basic Class Example Students
----

An example of how a class with attributes can be created, how objects of that class can be created (students), and how we can access each individual's student attribute(s)
