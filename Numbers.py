y = 5  #we declare y = 5
type(y) #we ask the type of the value (it will return int)

z = 2.5 #we declare z = 2.5
type(z) #we ask the type of the value (it will return float)

name="Chris" #we declare a string named Chris
type(name) #we ask the type of the value (it will return string)
name #we ask what we have stored inside the name variable

z=2.5E2 #we declare the value z=2.5 *100 =250.0
z #we ask what we have stored inside the z variable

#acceptable variable format in python
#a12345=5 is acceptable
#_12345=5 is acceptable
#_abcd=5 is acceptable
#12345_=5 is NOT acceptable